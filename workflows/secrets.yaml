# Optimus CRE Workflows – Vault DON Secrets
# Each key maps a logical secret name (used in {{.secretName}} templates)
# to one or more env var names.  The CRE CLI resolves them from .env
# during simulation and from the Vault DON in production.
secretsNames:
  backendApiKey:
    - BACKEND_API_KEY_ALL
  ethPrivateKey:
    - ETH_PRIVATE_KEY_ALL
